// Configuration
export const roomConfig = {
    maxParticipants: 4, // Maximum participants per room
    roomTimeoutDuration: 30 * 60 * 1000, // 30 minutes
    participantReconnectionWindow: 5 * 60 * 1000, // 5 minutes to reconnect
    cleanupInterval: 60 * 1000 // Check for cleanup every minute
}